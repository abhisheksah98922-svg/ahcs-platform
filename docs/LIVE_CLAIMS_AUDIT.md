# AHCS — Public Website Live Claims vs Reality Audit

This document audits every public claim made on the AHCS website against actual production implementation, database backing, and external integrations.

---

| # | Public Website Claim | Page / Component | Actual Implementation | Database-Backed | External Integration | Tested | Reality Status | Required Immediate Action |
|---|----------------------|------------------|-----------------------|-----------------|----------------------|--------|----------------|---------------------------|
| 1 | "Permanent Health ID (AHCS Client ID) — issued instantly" | `app/page.tsx`, `app/apply/page.tsx` | Client ID algorithm (ISO/IEC 7064) exists in `lib/client-id.ts`. However, issuing a "Permanent Health ID" instantly prior to real UIDAI / government document verification creates an unverified identity. In the UI, it was previously tied to a simulated officer screen or local submission. | Partial (`data/ahcs_production.json` via `lib/db/store.ts`, NOT PostgreSQL) | None (No UIDAI/ABHA API) | Yes (Local algorithm tests pass) | **PARTIAL** | Change to "Provisional Health ID issued upon mobile verification; Permanent ID upon official verification". Remove "instant" permanent claim. |
| 2 | "Doctor Appointments — Live now" | `app/page.tsx`, `app/providers/[id]/page.tsx` | Booking endpoint `POST /api/v1/appointments` exists and checks slot conflict against in-memory/JSON store. However, all listed hospitals and doctors are hardcoded fixture data (`data/ahcs_production.json`). No real clinic EHR or doctor sync exists. | No (In-memory/JSON only) | None | Yes (Unit test only) | **MOCK / DEMO** | Change website badge from "Live now" to "Coming Soon". Do not claim live OPD appointments when no real hospital is connected. |
| 3 | "Every prescription carries a code any pharmacy can verify" | Public marketing / Feature specs | No public pharmacy verification endpoint exists (`/api/v1/prescriptions/verify` or `/verify/prescription/[code]` is completely absent). Doctors can save medication lists in `app/api/v1/records`, but pharmacies have zero verification interface. | No | None | No | **MISSING** | Remove claim from public website until pharmacy portal and cryptographic verification endpoint are implemented. |
| 4 | "Emergency QR Gateway — Any treating facility can scan your QR" | `app/page.tsx`, `app/e/[token]/page.tsx`, `app/api/v1/emergency/[token]/route.ts` | The unauthenticated break-glass endpoint `/api/v1/emergency/[token]` genuinely resolves a dynamic 256-bit token to a minimal emergency dataset (blood group, allergies, contacts). Withholds clinical notes. Audit-logs scanner IP. However, data is read from `/tmp` JSON on Vercel, not PostgreSQL. | Partial (In-memory/JSON only) | None | Yes | **PARTIAL** | Connect to persistent database; website copy is functionally accurate to software logic, but persistence is serverless-ephemeral. |
| 5 | "Contactless NFC — Instant tap with any NFC-enabled smartphone" | `app/page.tsx`, `app/pricing/page.tsx`, `components/HealthCard.tsx` | Strictly a visual SVG/CSS icon on the digital card preview. No WebNFC API in browser, no ISO/IEC 14443 physical chip provisioning, no smart card factory integration. | No | None | No | **UI_ONLY** | Clearly label physical NFC card as "Future Physical Hardware" or remove claim of active contactless NFC functionality. |
| 6 | "Live SMS OTP Verification" | `app/apply/page.tsx`, `lib/auth/otp.ts` | Fast2SMS and Twilio client code exists in `lib/auth/otp.ts`, but credentials (`FAST2SMS_API_KEY`) are missing on production Vercel. System falls back to returning `devCode` in JSON response and displaying OTP on screen. | No (In-memory `Map`, lost on cold start) | Missing credentials | Yes (Fallback mode only) | **BROKEN / MISSING EXTERNAL CREDENTIALS** | Provide honest UI notice: "SMS Gateway not configured; enter displayed test code" OR insert real Fast2SMS API key in Vercel environment variables. |
| 7 | "Aadhaar / Identity Document Verification" | `app/apply/page.tsx`, `app/api/v1/verification/submit/route.ts` | File upload accepts files up to 10MB and computes SHA-256 hash. SHA-256 proves file integrity, NOT document authenticity. No connection to UIDAI, NSDL, or DigiLocker API. Citizen apply page previously contained simulated "Officer Ananya Sen" screen with self-approval. | Partial (In-memory/JSON store) | None (No UIDAI/NSDL gateway) | Yes | **MOCK / SIMULATED** | Status must remain `PENDING_VERIFICATION`. Remove simulated officer approval from citizen flow. State clearly that official verification requires manual officer desk or government e-KYC integration. |
| 8 | "PostgreSQL Database Backed Architecture" | `docs/*`, `.env`, `prisma/schema.prisma` | 30 Prisma models exist in `prisma/schema.prisma`, but NO production API route imports Prisma or connects to PostgreSQL. All 40+ routes import `db` from `lib/db/store.ts`, which reads/writes `data/ahcs_production.json` (or `/tmp/ahcs_data/ahcs_production.json` in serverless). | No (JSON file store) | None (PostgreSQL daemon not connected) | No (Postgres not running) | **BROKEN / NOT REAL PRODUCTION DATABASE** | Connect production runtime to real managed PostgreSQL (e.g. Supabase, Neon, or RDS) via Prisma Client. Remove file-based `/tmp` JSON dependency. |
| 9 | "Verified Provider Directory" | `app/providers/page.tsx`, `app/api/v1/providers/route.ts` | Displays list of hospitals and clinics. However, all listed institutions ("City Care Multi-Specialty Hospital", "Sunrise Clinic") are mock fixtures seeded in JSON with fictitious registration numbers ("KA-MED-HOSP-2018-0912"). | No (Seeded JSON fixtures) | None | Yes | **MOCK / DEMO** | Mark Provider Directory as "Partner Onboarding Open / Directory Preview" or remove fake hospital listings. |
| 10 | "Card Purchase & Subscription Checkout" | `app/pricing/page.tsx`, `app/api/v1/payments/*` | `POST /api/v1/payments/create-order` creates mock order string. `POST /api/v1/payments/verify` accepts a hardcoded `'mock_signature_approved'` string from frontend without opening Razorpay checkout or collecting actual funds. | Partial (Orders saved in JSON store) | Missing Razorpay live credentials | Yes (Simulated only) | **MOCK / SIMULATED PAYMENT** | Disable live payment success screens until real Razorpay key and webhook secrets are configured. Display "Payment Gateway in Test Mode". |
| 11 | "Free Forever" / Pricing Model Claims | `app/pricing/page.tsx`, `app/page.tsx` | Marketing statements claim certain tiers are "Free forever" while others require recurring subscriptions. | No | None | No | **BUSINESS / LEGAL PENDING** | Requires formal corporate legal disclaimer, privacy policy review, and published Terms of Service outlining pricing changes. |
